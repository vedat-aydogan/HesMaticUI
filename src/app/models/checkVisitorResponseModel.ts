export interface CheckVisitorResponseModel {
    expiration_date: string;
    current_health_status: string;
    masked_firstname: string;
    masked_identity_number: string;
    masked_lastname: string;
}